machine:
  environment:
    NPM_CONFIG_PROGRESS: false
  node:
    version: v5.6.0
general:
  artifacts:
    - "coverage"
dependencies:
  override:
    - npm install --no-optional
notify:
  webhooks:
    - url: http://bot.ambassify.com/publish/circleci/master
